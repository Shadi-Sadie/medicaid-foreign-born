"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = i,
variables_filter = list(
AGEP = 18:65, POVPIP = 000:139
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
return_vacant= True,
show_call= TRUE
)
ldf[[i]]$YEAR<-i
assign(paste0("ACS",i),ldf[[i]] )
}
ldf <- list() # creates a list
for( i in 2009)  {                            ## getting the data for year 2019 ##
ldf[[i]] <- get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = i,
variables_filter = list(
AGEP = 18:65, POVPIP = 000:139
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
return_vacant= TRUE,
show_call= TRUE
)
ldf[[i]]$YEAR<-i
assign(paste0("ACS",i),ldf[[i]] )
}
Data<-rbind(Data1,Data2)
classcol<-sapply(Data, class)
table(classcol)
char_columns <- which(classcol=="character")
for (i in char_columns){
Data[,i] <- as.integer(Data[,i])
}
Data<- Data %>% filter(POVPIP<139)   # Subset the income level only less than <139
table(Data$POVPIP, exclude = NULL)
table(ACS2009$POVPIP, exclude = NULL)
Data1<-read.csv("ss09pusa.csv", header = TRUE, sep ="," )
Data2<-read.csv("ss09pusb.csv", header = TRUE, sep =",")
Data<-rbind(Data1,Data2)
classcol<-sapply(Data, class)
table(classcol)
char_columns <- which(classcol=="character")
for (i in char_columns){
Data[,i] <- as.integer(Data[,i])
}
Data<- Data %>% filter(18<AGEP<65)   # Subset the income level only less than <139
Data<- Data %>% filter(18<AGEP& AGEP<65)   # Subset the income level only less than <139
Data<- Data %>% filter(POVPIP<139)   # Subset the income level only less than <139
Data<-rbind(Data1,Data2)
Data<- Data %>% filter(18<AGEP& AGEP<65)   # Subset the income level only less than <139
Data<- Data %>% filter(POVPIP<139)   # Subset the income level only less than <139
table(ACS2009$POVPIP, exclude = NULL)
table(Data$POVPIP, exclude = NULL)
class(ACS2009$POVPIP)
class(Data$POVPIP)
Data<-rbind(Data1,Data2)
class(Data$POVPIP)
class(Data$AGEP)
class(ACS2009$AGEP)
Data<- Data %>% filter(POVPIP<139)   # Subset the income level only less than <139
Data<- Data %>% filter(18<AGEP& AGEP<65)   # Subset the income level only less than <139
table(Data$AGEP, exclude = NULL)
table(ACS2009$AGEP, exclude = NULL)
Data<-rbind(Data1,Data2)
Data<- Data %>% filter(POVPIP<139)   # Subset the income level only less than <139
Data<- Data %>% filter(17<AGEP& AGEP<66)   # Subse
table(ACS2009$AGEP, exclude = NULL)
table(Data$AGEP, exclude = NULL)
table(Data$POVPIP, exclude = NULL)
table(ACS2009$POVPIP, exclude = NULL)
ACS<-ACS2009
ldf <- list() # creates a list
for( i in 2009)  {                            ## getting the data for year 2019 ##
ldf[[i]] <- get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = i,
variables_filter = list(
AGEP = 18:65, POVPIP = 000:139
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
return_vacant= TRUE,
show_call= TRUE
)
ldf[[i]]$YEAR<-i
assign(paste0("ACS",i),ldf[[i]] )
}
ACS<-get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = 2009,
variables_filter = list(
AGEP = 18:65, POVPIP = 000:139
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
return_vacant= TRUE,
show_call= TRUE
)
ACS<-get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = 2009,
variables_filter = list(
AGEP = 18:65, POVPIP = 000:139
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
return_vacant= TRUE
)
ACS<-get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = 2009,
variables_filter = list(
AGEP = 18:65, POVPIP = 000:139
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
show_call= TRUE
)
ACS<-get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = 2009,
variables_filter = list(
AGEP = 18:65, POVPIP = 000:139
),
survey = "acs1",
rep_weights = "person",
recode = TRUE,
show_call= TRUE
)
table(ACS2009$CITWP, exclude = NULL)
table(ACS2009$LANP, exclude = NULL)
class(ACS2009$LANP)
table(ACS2010$LANP, exclude = NULL)
table(Data$LANP, exclude = NULL)
Dat<-rbind(Data1,Data2)
table(Dat$AGEP, exclude = NULL)
table(Data$AGEP, exclude = NULL)
table(ACS2009$AGEP, exclude = NULL)
table(ACS2009$POVPIP, exclude = NULL)
table(Data$POVPIP, exclude = NULL)
table(Dat$POVPIP, exclude = NULL)
class(ACS2009$POVPIP)
b<-as.integer(ACS2009$POVPIP)
table(b, exclude = NULL)
b<-as.integer(ACS2010$POVPIP)
table(b, exclude = NULL)
class(ACS$POVPIP)
table(ACS$POVPIP, exclude = NULL)
ACS<- ACS %>% filter(POVPIP<139)   # Subset the income level only less than <139
table(ACS$POVPIP, exclude = NULL)
ACS<-ACS2009
ACS[ACS!="139"]
ACS<-ACS[!(ACS=="139"),]  # Removing the data of immigrant with less than 5 years residency
ACS<-ACS[!(ACS==139),]  # Removing the data of immigrant with less than 5 years residency
ACS<-ACS[!(ACS$POVPIP="139"),]  # Removing the data of immigrant with less than 5 years residency
ACS<-ACS[!(ACS$POVPIP=139),]  # Removing the data of immigrant with less than 5 years residency
ACS<-ACS2009
ACS<-ACS[!(ACS$POVPIP=="139"),]  # Removing the data of immigrant with less than 5 years residency
ACS<-ACS[!(ACS$AGEP=="18"),]  # Removing the data of immigrant with less than 5 years residency
ACS<-ACS[!(ACS$AGEP=="65"),]  # Removing the data of immigrant with less than 5 years residency
ACS2009<-ACS
ACS<-ACS2010
ACS<- ACS %>% filter(POVPIP<139)
ACS<-ACS2010
table(ACS$POVPIP, exclude = NULL)
ACS<-ACS[!(ACS$POVPIP=="139"),]  # Removing the data of immigrant with less than 5 years residency
ACS<-ACS[!(ACS$AGEP=="65"),]
ACS<-ACS[!(ACS$AGEP=="18"),]  # Removing the data of immigrant with less than 5 years residency
ACS2010<-ACS
table(ACS$POVPIP, exclude = NULL)
table(ACS$AGEP, exclude = NULL)
table(ACS$LANP, exclude = NULL)
Data<-Data[!(Data$POVPIP=="139"),]  # Removing the data of immigrant with less than 5 years residency
Data<-Data[!(Data$AGEP=="18"),]  # Removing the data of immigrant with less than 5 years residency
Data<-Data[!(Data$AGEP=="65"),]  # Removing the data of immigrant with less than 5 years residency
rm(b)
rm(char_columns)
rm(fileUrl)
rm(temp)
rm(temp2)
rm(i)
rm(classcol)
rm(Data1)
rm(Data2)
rm(Dat)
rm(ACS)
table(ACS2009$YEAR, exclude = NULL)
table(ACS2010$YEAR, exclude = NULL)
table(Data$YEAR, exclude = NULL)
ldf <- list() # creates a list
for( i in 2011:2014)  {                            ## getting the data for year 2019 ##
ldf[[i]] <- get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = i,
variables_filter = list(
AGEP = 19:64, POVPIP = 000:138
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
)
ldf[[i]]$YEAR<-i
assign(paste0("ACS",i),ldf[[i]] )
}
for( i in 2015:2019)  {                            ## getting the data for year 2019 ##
ldf[[i]] <- get_pums(
variables = c ("REGION","AGEP", "CIT" , "ENG" , "FER","HINS1",
"HINS2", "HINS3" , "HINS4","HINS5", "HINS6", "HINS7", "LANX",
"MAR", "SCHL", "SEX", "YOEP", "ANC1P","DIS","HICOV", "ESR",
"LANP", "NATIVITY","HISP", "POBP", "POVPIP", "PUBCOV","PRIVCOV", "RAC1P"),
state = "all",
year = i,
variables_filter = list(
AGEP = 19:64, POVPIP = 000:138
),
survey = "acs1",
rep_weights = "person",
recode = FALSE,
)
ldf[[i]]$YEAR<-i
assign(paste0("ACS",i),ldf[[i]] )
}
ACS<-rbind(ACS2009,ACS2010,ACS2011,ACS2012,ACS2013,ACS2014,ACS2015,ACS2016,ACS2017,ACS2018,ACS2019)
setwd("~/Downloads/ACS Data")
write.csv(ACS, file = "RAWACS.csv", row.names = FALSE)
table(ACS$POVPIP, exclude = NULL)
table(ACS$LANP, exclude = NULL)
colnames(ACS)
df = subset(ACS, select = -c(ANC1P,
LANP,
LANX,
POBP,
PUBCOV,
PRIVCOV) )
write.csv(df, file = "RAWACS.csv", row.names = FALSE)
ACS<-df
setwd("~/Paper-1-Cancer-Screening-and-Immigrants")
setwd("~/Paper-1-Cancer-Screening-and-Immigrants/Codes/R")
RAWACS<-ACS
ACS209Net<-Data
Data<-ACS
table(Data$AGEG, exclude = NULL)
table(Data$AGEP, exclude = NULL)
classcol<-sapply(Data, class)
table(classcol)
char_columns <- which(classcol=="character")
for (i in char_columns){
Data[,i] <- as.integer(Data[,i])
}
Data<-ACS
classcol<-sapply(Data, class)
table(classcol)
char_columns <- which(classcol=="character")
for (i in char_columns){
Data[,i] <- as.numeric(Data[,i])
}
classcol<-sapply(Data, class)
table(classcol)
Data=as.data.frame.matrix(df)
classcol<-sapply(Data, class)
table(classcol)
classcol<-sapply(Data, class)
table(classcol)
View(Data)
head(Data, 10)
Data<-df
Data=as.data.frame(df)
classcol<-sapply(Data, class)
table(classcol)
char_columns <- which(classcol=="character")
for (i in char_columns){
Data[,i] <- as.numeric(Data[,i])
}
table(Data$AGEP, exclude = NULL)
table(df$AGEP, exclude = NULL)
table(ACS$AGEP, exclude = NULL)
class(ACS$AGEP)
class(Data$AGEP)
table(Data$REGION, exclude = NULL)
table(ACS$REGION, exclude = NULL)
table(Data$ST, exclude = NULL)
table(ACS$ST, exclude = NULL)
table(Data$CIT, exclude = NULL)
table(ACS$CIT, exclude = NULL)
table(Data$ENG, exclude = NULL)
table(ACS$ENG, exclude = NULL)
table(Data$FER, exclude = NULL)
table(ACS$FER, exclude = NULL)
table(Data$HINS1, exclude = NULL)
table(ACS$HINS1, exclude = NULL)
table(Data$HINS2, exclude = NULL)
table(ACS$HINS2, exclude = NULL)
table(Data$HINS3, exclude = NULL)
table(ACS$HINS3, exclude = NULL)
table(Data$HINS4, exclude = NULL)
table(ACS$HINS4, exclude = NULL)
table(Data$HINS5, exclude = NULL)
table(ACS$HINS5, exclude = NULL)
table(Data$HINS6, exclude = NULL)
table(ACS$HINS6, exclude = NULL)
table(Data$HINS7, exclude = NULL)
table(ACS$HINS7, exclude = NULL)
table(Data$MAR, exclude = NULL)
table(ACS$MAR, exclude = NULL)
table(Data$SCHL, exclude = NULL)
table(ACS$SCHL, exclude = NULL)
table(Data$SEX, exclude = NULL)
table(ACS$SEX, exclude = NULL)
table(Data$YOEP, exclude = NULL)
table(ACS$YOEP, exclude = NULL)
table(Data$DIS, exclude = NULL)
table(ACS$DIS, exclude = NULL)
table(Data$HICOV, exclude = NULL)
table(ACS$HICOV, exclude = NULL)
table(Data$HISP, exclude = NULL)
table(ACS$HISP, exclude = NULL)
table(Data$ESR, exclude = NULL)
table(ACS$ESR, exclude = NULL)
table(Data$NATIVITY, exclude = NULL)
table(ACS$NATIVITY, exclude = NULL)
table(Data$POVPIP, exclude = NULL)
table(ACS$POVPIP, exclude = NULL)
table(Data$RAC1P, exclude = NULL)
table(ACS$RAC1P, exclude = NULL)
table(ACS$FER, exclude = NULL)
table(Data$FER, exclude = NULL)
table(Data$FER,Data$FER, exclude = NULL)
table(Data$FER,Data$YEAR, exclude = NULL)
table(ACS$FER,ACS$YEAR, exclude = NULL)
table(ACS$ST,ACS$YEAR, exclude = NULL)
ldf <- list() # creates a list
for( i in 2009:2019)  {
ldf[[i]] <- get_pums(
variables = c ("RELP"),
state = "all",
year = i,
variables_filter = list(
AGEP = 19:64, POVPIP = 000:138 ## Filtering age and income #
),
survey = "acs1",
recode = FALSE,
)
ldf[[i]]$YEAR<-i ## Creat year and assigne value ##
assign(paste0("A",i),ldf[[i]] )
}
for( i in 2009:2019)  {
ldf[[i]] <- get_pums(
variables = c ("TYPE"),
state = "all",
year = i,
variables_filter = list(
AGEP = 19:64, POVPIP = 000:138 ## Filtering age and income #
),
survey = "acs1",
recode = FALSE,
)
ldf[[i]]$YEAR<-i ## Creat year and assigne value ##
assign(paste0("A",i),ldf[[i]] )
}
View(A2009)
A<-rbind(A2009,A2010,A2011,A2012,A2013,A2014,A2015,A2016,A2017,A2018,A2019)
B<-A$TYPE
B<-as.numeric(B)
Data$TYPE<-B
table(Data$TYPE,ACS$YEAR, exclude = NULL)
table(Data$TYPE, exclude = NULL)
table(A$TYPE, exclude = NULL)
table(A2009$TYPE, exclude = NULL)
table(ACS209Net$TYPE, exclude = NULL)
table(ACS209Net$POVPIP, exclude = NULL)
table(ACS$POVPIP, exclude = NULL)
table(Data$POVPIP, exclude = NULL)
table(Data$TYPE, exclude = NULL)
Data<-Data[!(Data$TYPE %in% c(2,3)),]
table(Data$TYPE, exclude = NULL)
table(Data$POVPIP, exclude = NULL)
table(Data$YEAR, exclude = NULL)
table(Data$YOEP, exclude = NULL)
Data$YLIU<-0                      # create variable YLIU year lived in US to use for removing immigrant with less than 5 years
Data$YLIU<-Data$YEAR-Data$YOEP
table(Data$YLIU, exclude = NULL) # l
table(ACS$YOEP, exclude = NULL)
table(ACS$YOEP, ACS$YEAR, exclude = NULL)
class(ACS$YOEP)
table(ACS2009$YOEP,  exclude = NULL)
table(ACS209Net$YOEP,  exclude = NULL)
class(ACS2009$YOEP)
table(ACS209Net$YOEP,  exclude = NULL)
table(ACS2009$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2009 & Data$YOEP==1918,NA,Data$YOEP) # 1. 25-34
table(ACS2010$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2010 & Data$YOEP==1918,NA,Data$YOEP) # 1. 25-34
table(ACS2011$YOEP,  exclude = NULL)
class(ACS2011$YOEP)
table(ACS2011$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2011 & Data$YOEP==1918,NA,Data$YOEP) #
table(ACS2012$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2012 & Data$YOEP==1920,NA,Data$YOEP) #
table(ACS2013$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2013 & Data$YOEP==1920,NA,Data$YOEP) #
table(ACS2014$YOEP,  exclude = NULL)
table(ACS2014$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2014 & Data$YOEP==1920,NA,Data$YOEP) #
table(ACS2015$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2015 & Data$YOEP==1920,NA,Data$YOEP) #
table(ACS2016$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2016 & Data$YOEP==1920,NA,Data$YOEP) #
table(ACS2017$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2017 & Data$YOEP==1924,NA,Data$YOEP) #
table(ACS2018$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2018 & Data$YOEP==1928,NA,Data$YOEP) #
table(ACS2019$YOEP,  exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2019 & Data$YOEP==1928,NA,Data$YOEP) #
table(Data$YEAR=,  exclude = NULL)
table(Data$YEAR,  exclude = NULL)
table(Data$YEAR, Data$YOEP exclude = NULL)
table(Data$YEAR, Data$YOEP, exclude = NULL)
table(Data$YEAR, Data$NATIVITY, exclude = NULL)
table(Data$YEAR, Data$CIT, exclude = NULL)
Data$YLIU<-0                      # create variable YLIU year lived in US to use for removing immigrant with less than 5 years
Data$YLIU<-Data$YEAR-Data$YOEP
table(Data$YLIU, exclude = NULL) #
table(Data$YLIU,Data$CIT, exclude = NULL) # looking at the table of years living in US
table(Data$CIT, Data$YLIU, exclude = NULL) # looking at the table of years living in US
Data<-Data[!(Data$YLIU<5 & Data$CIT==5),]  # Removing the data of immigrant with less than 5 years residency
table(Data$CIT, Data$YLIU, exclude = NULL) # looking at the table of years living in US by citizenshio status.
class(ACS$YOEP)
table(ACS$YOEP)
Data<-ACS
Data=as.data.frame(Data)
classcol<-sapply(Data, class)
table(classcol)
char_columns <- which(classcol=="character")
for (i in char_columns){
Data[,i] <- as.numeric(Data[,i])
}
Data$TYPE<-B
table( Data$YOEP, exclude = NULL)
table(Data$YEAR, Data$CIT, exclude = NULL)
Data$YOEP<-ifelse(Data$YEAR==2009 & Data$YOEP==1918,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2010 & Data$YOEP==1918,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2011 & Data$YOEP==1918,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2012 & Data$YOEP==1920,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2013 & Data$YOEP==1920,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2014 & Data$YOEP==1920,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2015 & Data$YOEP==1920,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2016 & Data$YOEP==1920,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2017 & Data$YOEP==1924,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2018 & Data$YOEP==1928,NA,Data$YOEP) #
Data$YOEP<-ifelse(Data$YEAR==2019 & Data$YOEP==1928,NA,Data$YOEP) #
table( Data$YOEP, exclude = NULL)
table( Data$CIT, exclude = NULL)
write.csv(Data, file = "RAWACS.csv", row.names = FALSE)
setwd("~/Downloads/ACS Data")
write.csv(Data, file = "RAWACS.csv", row.names = FALSE)
table( Data$TYPE, exclude = NULL)
Data<-Data[!(Data$TYPE %in% c(2,3)),]
table( Data$TYPE, exclude = NULL)
Data$YLIU<-0                      # create variable YLIU year lived in US to use for removing immigrant with less than 5 years
Data$YLIU<-Data$YEAR-Data$YOEP
table(Data$YLIU, exclude = NULL) # looking at the table of years living in US
table(Data$CIT, Data$YLIU, exclude = NULL) #
Data<-Data[!(Data$YLIU<5 & Data$CIT==5),]  # Removing the data of immigrant with less than 5 years residency
write.csv(Data, file = "PREACS.csv", row.names = FALSE)
setwd("~/Downloads/ACS Data")
write.csv(Data, file = "PREACS.csv", row.names = FALSE)
Data<- Data %>% filter(AGEP<65 & AGEP>25)
table( Data$AGEP, exclude = NULL)
Data<-Data[!(Data$ST %in% c(25,41)),]  # Looked up State FIPS code MA is 25 and OR is 41
write.csv(Data, file = "PREACS.csv", row.names = FALSE)
setwd("~/Paper-1-Cancer-Screening-and-Immigrants")
setwd("~/Paper-1-Cancer-Screening-and-Immigrants/Codes/R")
